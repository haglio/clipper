{
  "_comment": "Placeholder content overlay. The real values live in content.local.json (git-ignored). suite_root is the folder holding the media library. project_roots is optional: the folders holding the sibling app checkouts, searched in order, for when they do not live under suite_root/projects; omit it and that is exactly where they are looked for.",
  "suite_root": "C:/path/to/suite-root"
}
